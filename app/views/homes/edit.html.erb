<h1>Editing Book</h1>
<%= form_for(@book, url: book_path{book.id} ) do |f| %>
<% if @book.errors.any? %>
  <div>
  	<p>
  		<%= pluralize(@book.errors.full_messages.count, "error") %> prohibited this book from being saved:
	</p>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
	<p>Title</p>
	<%= f.text_field :title %>
	<p>Body</p>
	<%= f.text_area :body %>
	<p><%= f.submit 'Update Book' %></p>
<% end %>
<%= link_to "Show", book_path(@book.id) %>
<%= link_to "Back", books_path %>